.nested-fields
  = f.hidden_field :type, value: "ExclusionZone"
  = f.input :address, label: "Street address", input_html: { class: "address" }
  = f.input :radius, label: "Exclusion radius (miles)"
  = link_to_remove_association "remove exclusion zone", f
